# 系统拨乱反正与后续计划

本文档旨在清晰地阐述我们当前的调试成果、系统现状，并提供一份详细、完整的代码修改计划，以统一我们对系统架构的理解，并指导后续的开发工作。

---

## 1. 我们的成果和现状

经过上一轮的联合调试，我们取得了决定性的进展，并对系统现状有了清晰的认识。

### 核心成果

**我们通过 `ffmpeg` 测试，无可辩驳地证明了核心音频输入链路是完全通畅的。**

这具体意味着：
1.  **网络验证成功：** 从您的笔记本 (`192.168.3.7`) 到 Jetson (`192.168.3.10`) 的网络连接是通畅的。
2.  **Docker `host` 模式验证成功：** Jetson 上的 Docker 容器已正确配置为 `host` 网络模式，能够直接使用 Jetson 的物理网络。
3.  **端口监听验证成功：** 在 Jetson 容器内运行的 `MicrophoneThread` 服务，确实在 `0.0.0.0:8888` 上成功监听，并能接收来自局域网的外部连接。
4.  **音频流验证成功：** 格式为 `16kHz, 16-bit, 单声道` 的 PCM 音频裸流，可以从笔记本被稳定地捕获、发送、并被 Jetson 容器内的 Python 脚本接收。

**结论：** 第二阶段的核心目标——打通“语音输入”通路——已经**技术性达成**。我们现在拥有一个经过验证的、可靠的音频输入管道。

### 系统现状

我们也清晰地认识到，当前代码库中的一些脚本与我们最终确定的架构存在冲突，这正是之前连接失败的根源。

1.  **架构已明确：**
    *   **输入流 (笔记本 -> Jetson):** `笔记本麦克风客户端` -> `Jetson上的MicrophoneThread服务器 (端口 8888)`
    *   **输出流 (Jetson -> 笔记本):** `Jetson上的SpeechSystem客户端` -> `笔记本扬声器服务器 (端口 9889)`
2.  **代码待修正：**
    *   `sensors/audio_client_tcp.py`: 功能混乱，试图同时处理输入和输出，且其输出逻辑（作为客户端）与架构不符。
    *   `intelligence/speech.py`: 功能不完整，合成语音后尝试在无扬声器的 Jetson 容器内本地播放，而不是通过网络发送到笔记本。
    *   `utils/config.py` & `config.ini`: 缺乏对远程扬声器（笔记本 IP 和端口）和讯飞 TTS 服务密钥的配置支持，导致代码中存在硬编码。

我们目前的任务就是“拨乱反正”，将代码与我们已验证的、清晰的架构对齐。

---

## 2. 详细代码修改思路

我将对以下三个文件进行一次性、完整的修改，以实现我们的目标架构。

### A. 修正 `utils/config.py` 和 `config.ini`

*   **修改原因：**
    为了消除代码中的硬编码（如科大讯飞的密钥、笔记本IP等），并让系统配置化、更易于维护，我们需要一个统一的配置中心。

*   **修改思路：**
    1.  在 `utils/config.py` 中，重新定义一个 `@dataclass class SpeechConfig`。
    2.  在这个类中，添加以下字段：
        *   `notebook_ip: str`: 用于指定笔记本的IP地址。
        *   `speaker_port: int`: 用于指定笔记本上扬声器服务的监听端口。
        *   `app_id: str`, `api_key: str`, `api_secret: str`: 用于存放科大讯飞TTS服务的认证信息。
    3.  将 `SpeechConfig` 添加到主配置类 `AppConfig` 中。
    4.  在 `load_config` 函数中，添加入对 `.ini` 文件中 `[speech]` 配置段的解析逻辑。
    5.  **与此对应，您需要在 `config.ini` 文件中添加以下内容：**
        ```ini
        [speech]
        notebook_ip = 192.168.3.7
        speaker_port = 9889
        app_id = aeb60378
        api_key = e248b59b7b21d7291702b7808ba07257
        api_secret = MjQ1ZmM3MjkwNmEzZTQyN2ZiNTYxN2Ey
        ```

### B. 修正 `sensors/audio_client_tcp.py`

*   **修改原因：**
    当前脚本功能混乱，是导致之前连接失败的直接原因。我们需要一个纯粹、可靠的工具，只负责将笔记本的麦克风音频发送出去。

*   **修改思路：**
    1.  **功能简化：** 将其重构为一个**纯粹的麦克风音频发送客户端**。
    2.  **移除输出逻辑：** 删除所有与扬声器相关的代码，包括 `speaker_port`、`speaker_socket`、`output_stream`、`output_queue` 以及音频接收线程。
    3.  **专注核心：** 脚本的唯一职责将是：初始化 `sounddevice` -> 连接到 Jetson 的 `8888` 端口 -> 持续捕获音频并发送。
    4.  这样修改后，它将不再尝试连接不存在的 `9889` 端口，可以独立、稳定地工作。

### C. 修正 `intelligence/speech.py`

*   **修改原因：**
    这是实现“语音输出”通路的核心改动。当前脚本在错误的设备（Jetson）上尝试播放音频，我们需要让它把音频数据发送到正确的设备（笔记本）。

*   **修改思路：**
    1.  **角色转变：** `SpeechSystem` 在播放音频时，其角色将从“本地播放器”转变为一个“**网络客户端**”。
    2.  **添加网络能力：** 我会为其增加一个内部的 TCP 客户端模块。这个模块的功能是在需要时，连接到 `SpeechConfig` 中指定的 `notebook_ip:speaker_port`。
    3.  **改造 `say` 方法：**
        *   该方法将接受 `SpeechConfig` 作为初始化参数。
        *   当科大讯飞API成功返回合成的 PCM 音频数据后，原有的本地播放代码 (`_play_audio_sync`) 将被**替换**。
        *   取而代之的，是调用内部TCP客户端，将音频数据通过网络发送给笔记本上的扬声器服务。
    4.  **清理无用代码：** 所有与本地播放相关的逻辑（如 `aplay`, `paplay` 的检测）都将被移除，使代码更清晰。

---

完成以上所有修改后，我们的代码将与架构完全对齐，音频的输入和输出通路都将变得清晰、可测试、可运行。届时，我们就可以自信地进入第三和第四阶段的完整系统联调。